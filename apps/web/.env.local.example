NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
# 1 = reset game state on every page refresh (dev only)
NEXT_PUBLIC_DEBUG_FRESH=0
# 1 = city mode runs fully offline (fixtures + fake opponents); ?reveal=1 forces the result
NEXT_PUBLIC_CITY_LOCAL=0
# 1 = release city mode in production (show Steden tab + /city); local/preview always on
NEXT_PUBLIC_CITY_ENABLED=0
